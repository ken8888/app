create table weeklymeeting(
id int,
type varchar(10),
building varchar(10),
room varchar(10),
attendance varchar(10),
start_date varchar(10),
end_date varchar(10),
day varchar(10),
start_time int,
end_time int,
primary key (id)
);